{
  "name": "ctc-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc && copyfiles -u 1 src/**/function.json dist && shx cp host.json dist/host.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@azure/functions": "^3.5.1",
    "undici": "^7.16.0"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "concurrently": "^9.2.1",
    "copyfiles": "^2.4.1",
    "cpx": "^1.5.0",
    "shx": "^0.4.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
